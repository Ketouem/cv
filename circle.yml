machine:
  services:
    - docker

general:
  artifacts:
    - "./dist"

compile:
  override:
    - make build

test:
  override:
    - test -s dist/cv.pdf

deployment:
  prod:
    branch: master
    commands:
      - aws s3 cp --acl public-read ./dist/cv.pdf s3://public-dist/cv.pdf
